# Explicitly allow build scripts only for trusted native modules
# This is more secure than enable-pre-post-scripts=true (which allows ALL packages)
# Only these packages can execute code during installation
only-built-dependencies[]=isolated-vm
only-built-dependencies[]=bcrypt
only-built-dependencies[]=sharp
