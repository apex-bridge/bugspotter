# Admin Panel Environment Configuration

# API URL - Update for your deployment
# For same-domain deployment (admin.example.com + api.example.com via reverse proxy):
VITE_API_URL=/api

# For cross-origin deployment:
# VITE_API_URL=https://demo.api.bugspotter.io
