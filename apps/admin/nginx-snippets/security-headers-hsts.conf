# HSTS: Enforce HTTPS for 1 year, include subdomains, allow preload list submission
# Note: Only effective when served over HTTPS (TLS termination at load balancer)
# Included in production configurations only (not in dev)
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
